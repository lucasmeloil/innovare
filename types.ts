export interface CollectionItem {
  id: number;
  title: string;
  image: string;
}

export interface TestimonialItem {
  id: number;
  text: string;
  author: string;
  location: string;
  image: string;
}

export interface NavLink {
  label: string;
  href: string;
}